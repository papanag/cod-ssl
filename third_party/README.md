# Official backbone repositories

Keep Meta repositories outside this tracked tree and point to them with
`DINOV3_REPO_DIR` and `VJEPA2_REPO_DIR`. Checkpoints are explicit local files
configured through `DINOV3_WEIGHTS` and `VJEPA21_WEIGHTS`; never commit signed URLs.

