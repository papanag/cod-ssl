experiment:
  seed: 42
  input_size: 384
model:
  backbone:
    name: PLACEHOLDER
    layers: [2, 5, 8, 11]
  decoder:
    name: common_cod_decoder
    projection_dim: 128
loss:
  name: bce_soft_iou
  smooth: 1.0
data:
  train_manifest: manifests/train_all.csv
  batch_size: 4
  num_workers: 2
training:
  epochs: 40
  optimizer: adamw
  learning_rate: 0.001
  weight_decay: 0.0001
  scheduler: cosine
  gradient_accumulation_steps: 4
  amp: true
  amp_dtype: auto
  grad_clip_norm: 1.0
  checkpoint_every: 1
